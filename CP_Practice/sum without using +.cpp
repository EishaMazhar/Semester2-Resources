#include<iostream>
using namespace std;
main()
{
	int a,b,s,sum;
	cout<<"enter the a : ";
	cin>>a;
	cout<<"enter the b : ";
	cin>>b;
    s=-(a)-(b);
    sum=s*(-1);
    cout<<endl<<"the sum is "<<sum;
}
